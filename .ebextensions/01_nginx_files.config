files:
  "/etc/nginx/sites-enabled/eb_nginx_status.conf" :
    mode: "000755"
    owner: root
    group: root
    content: |
        server {
          listen 80;
          location /eb_nginx_status {
            stub_status on;
            access_log   off;
          }
        }
